database=postgres
#spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://localhost/petclinic}
#spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://127.0.0.1/petclinic}

spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://10.244.231.224/petclinic}

spring.datasource.username=${POSTGRES_USER:dbuser}
spring.datasource.password=${POSTGRES_PASS:securepassword123}

#spring.datasource.username=${POSTGRES_USER:petclinic}
#spring.datasource.password=${POSTGRES_PASS:petclinic}

# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
